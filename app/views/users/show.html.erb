<script>
 var allMarkers = <%=raw @hash.to_json %>;
 var allMarkersTwo = <%=raw @hash_two.to_json %>;
</script>

<div class="row">

<div class="col-md-4">
  <h1 class="page-header"><%= @user.name %></h1>
  <%= image_tag @user.image_url, class: "img-circle img-responsive" %><br>
  <p><%= @user.email %></p><br>
  <p><%= @user.location %></p><br>
  <p><%= @user.bio%></p><br>
  Current Location: <strong><%= @user.current_location %></strong>
</div>
<% if current_user == @user %>
  <div class="col-md-8">
    <div id="map" style='width: 100%; height: 40em;'></div>
  </div>
<% end %>
 <div class="col-md-12">
   <table class="table table-hover table-responsive">
     <thead>
       <tr>
         <h3>Attractions near you</h3>
       </tr>
     </thead>
     <tbody>
       <% @spots.each do |spot| %>
         <% present spot do |spot_presenter| %>
           <tr>
             <td><%= spot_presenter.image_url %></td>
             <td><h3><%= spot_presenter.name %></h3></td>
             <td><strong><%= spot_presenter.attraction_types %></strong>
             <strong><%= spot_presenter.attraction_status %></strong>
             <strong><%= spot_presenter.attraction_price %></strong>
             Rating: <%= spot_presenter.attraction_rating %></td>
           </tr>
         <% end %>
       <% end %>
     </tbody>
   </table>
   <%# will_paginate @corrals_to_view, previous_label: h("<"), next_label: h(">") %>
 </div>

 <!-- <div class="modal fade" id="myModal">
     <div class="modal-dialog">
         <div class="modal-content">
           <div class="container">
             <% flash.each do |key, value| %>
               <div class="alert alert-dismissable" data-dismiss="alert" aria-hidden="true" style="cursor: pointer">
                 <%= content_tag(:div, value, class: "alert alert-#{key}") %>
               </div>
             <% end %>
           </div>
         </div>
     </div>
 </div> -->
